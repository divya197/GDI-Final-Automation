package com.gdi.useractions;

import org.openqa.selenium.WebElement;

import com.gdi.functionality.LoginPage;
import io.appium.java_client.android.AndroidDriver;

public class ActionMapper {
	
	public static void executeAction(String actionName, WebElement ele){
		if(functionName.equalsIgnoreCase("login"))
		{
			LoginPage login=new LoginPage(androidDriver);
		}
		
	}
	
}
